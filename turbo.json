{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "outputs": ["dist/**"],
      "inputs": ["apps/chrome-extension/src/**/*"],
      "cache": true
    },
    "type-check": {}
  }
}
